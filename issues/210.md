Write a script that uses:  

```
imgquotio.py PROMPT --model gpt --output OUTPUTPATH --size 1024x1024
```

to generate one image for every post in the blog. Output them into src_docs/md/media/illu/ and make them one consistent pencil sketch style. Ultrathink to best capture the essence of the post. Look at each result and re-generate if necessary. 

Then study reference/mkdocs-materialx to understand how the blog plugin lets us place the images in the blog index, and incorporate them there, as thumbnails on the right side of the blog post. Also incorporate them reasonably in the post content. Remember that you can use Markdown with `{ .tailwind-class .tailwind-class }` 

Work tirelessly until you finish everything. Don’t prompt for complex things. Be inventive. Remember that these pencil sketches need to be one consistent style, and they need to optically work in quite small sizes as a thumbnail. 

Write the script in Python that calls the imgquotio.py script to generate the images. The script needs to work async, and check if the image already exists before generating it, and check if the image has succeeded, and retry using tenacity if not. There should be lightweight parallelization (like 3 concurrent tasks) and retries.

Then run the script, check the results, and keep incorporating. 