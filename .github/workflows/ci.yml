# this_file: .github/workflows/ci.yml
name: deploy

on:
  push:
    branches:
      - main
    tags:
      - 'v*.*.*'
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    if: github.event.repository.fork == false
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: latest
          enable-cache: true
          cache-dependency-glob: pyproject.toml

      - name: Set up Python
        run: uv python install 3.12

      - name: Sync dependencies
        run: uv sync --frozen

      - name: Build site
        run: ./build.sh build

      - name: Sanity-check output
        run: |
          [[ -f docs/index.html ]] || exit 1
          [[ -f docs/about/index.html ]] || exit 1
          [[ -f docs/CNAME ]] || exit 1
          [[ -f docs/.nojekyll ]] || exit 1
          [[ -f docs/llms.txt ]] || exit 1
          [[ -f docs/llms-full.txt ]] || exit 1

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs
          publish_branch: gh-pages
          cname: blog.fontlab.com
          keep_files: false
          force_orphan: true
          user_name: 'github-actions[bot]'
          user_email: 'github-actions[bot]@users.noreply.github.com'
          commit_message: 'deploy: ${{ github.ref_name }}'
